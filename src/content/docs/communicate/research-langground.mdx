---
title: "Research Connection: LangGround"
description: "How LangGround anchors learned protocols to human language to support interpretability and zero-shot cooperation."
related:
  - slug: communicate/interpretable-communication
    icon: cycle
  - slug: adapt/zero-shot-coordination
    icon: users-round
  - slug: resources/references
    icon: book
---

import Callout from '../../../components/ui/Callout.astro';
import Term from '../../../components/ui/Term.astro';
import Equation from '../../../components/equations/Equation.astro';
import KnowledgeCheck from '../../../components/ui/KnowledgeCheck.astro';
import Objectives from '../../../components/ui/Objectives.astro';
import SectionMeta from '../../../components/ui/SectionMeta.astro';

<SectionMeta />

<Callout variant="predict" title="By the end of this section you can answer">
Can a learned protocol stay useful and still be understandable?
</Callout>

LangGround illustrates how learned communication can remain useful while its semantics are anchored to human language.

<Objectives>

- State the transfer problem created by private emergent conventions
- Describe how language grounding supplies an external reference space
- Report what the authors measured, and on which axes
- Extract the design principle that generalizes beyond the paper

</Objectives>

## Private Protocols and Transfer

**Language Grounded Multi-agent Reinforcement Learning with
Human-interpretable Communication** (NeurIPS 2024) opens by noting that MARL
methods can enable agents to learn a shared communication protocol from
scratch and accomplish challenging team tasks, but that the learned language
is **usually not interpretable to humans or to other agents not co-trained
together**, which limits its applicability in ad hoc teamwork.

That is the two-part finding from *Interpretable Communication*, arrived at
independently: opacity to people, and incompatibility with agents you did not
train with. The second is the one with teeth, and it is why this paper belongs
at the end of a communication chapter rather than in a section on
explainability.

## Language Grounding

The idea, at the level worth carrying:

> Rather than leaving the communication representation entirely
> unconstrained, align the agents' communication space with an embedding space
> derived from human natural language.

Concretely, the authors ground agent communications on **synthetic data
generated by embodied large language models** placed in interactive teamwork
scenarios. The language model supplies examples of what a helpful message
looks like in a given situation, and the agents' message space is shaped to
sit in the same embedding space as those examples.

<Equation
  label="Figure 1"
  wide
  tex={String.raw`\ubrace{comm}{\text{agent message space}}{learned} \;\longleftrightarrow\; \ubrace{observe}{\text{natural language embedding space}}{fixed, external}`}
  terms={[
    { tex: String.raw`\text{agent message space}`, is: 'the representation optimized for cooperative action', tone: 'comm' },
    { tex: String.raw`\text{language embedding space}`, is: 'the shared external semantic reference', tone: 'observe' },
  ]}
  caption="Grounding aligns learned messages with an external semantic reference."
/>

Read that as the general move rather than as an architecture. *Learning
Communication Protocols* showed that a learned symbol's meaning lives in the
pair of policies, which is precisely why it does not transfer. Grounding gives
the meaning somewhere else to live.

<Callout tone="observe" title="Not an LLM lesson" icon="compass">
The large language model here is a source of grounding data, not the
communication mechanism. The agents are not talking to each other in English
at run time, and nothing in this resource requires you to know how the
language model works.

What matters is the structure: a learned representation constrained to align
with an external, shared one.
</Callout>

## Reported Experimental Findings

Three results, and they are worth stating separately because they answer
different objections.

**Task performance is maintained.** Grounding constrains what the agents may
learn, so the obvious worry is that it costs capability. The authors report
that introducing language grounding does not reduce task performance.

**Communication emerges faster.** They also report that grounding
*accelerates* the emergence of communication. This is the more interesting of
the two: the bootstrapping problem from *Learning Communication Protocols* is that neither sender nor
receiver can be correct first, and an external anchor gives both of them a
target that does not depend on the other having already succeeded.

**The protocols generalise zero-shot.** The learned communication protocols
exhibit zero-shot generalization in ad hoc teamwork scenarios with **unseen
partners and novel task states**.

<Callout variant="takeaway">
That third result is the direct answer to the failure at the end of
*Interpretable Communication*, where two individually excellent agents failed
together because their conventions disagreed.

If both agents' protocols are anchored to the same external space, they are no
longer free to disagree arbitrarily. The convention stops being private to a
pair.
</Callout>

<Callout tone="observe" title="Published results, not ours" icon="compass">
Everything in this section is what the paper reports, on its environments and
its baselines. Nothing here was measured in this resource, and the Challenge
Lab does not reproduce it.

Read the paper before relying on any of these claims in your own work, and
note in particular that "maintains task performance" and "accelerates
emergence" are claims about specific tasks rather than guarantees.
</Callout>

## Implications for Cooperative Communication

This is the question to leave the chapter with:

> **Should a communication protocol merely work, or should it also be
> understandable by agents that were not trained with it?**

There is a real cost either way. An unconstrained protocol is free to find
whatever encoding suits the task, and is private to the pair that found it. A
grounded protocol gives up some of that freedom for a chance at being read.

Which you want depends on whether your agents will ever meet a stranger. That
is the Adapt chapter.

<KnowledgeCheck
  id="cm-2-8-grounding"
  type="choice"
  question="Why would grounding a message space in an external representation help with unseen partners?"
  options={[
    {
      text: 'Because the meaning is anchored to something both agents can share independently, rather than to a convention private to one training pair.',
      correct: true,
      feedback: 'Exactly. *Learning Communication Protocols* showed a learned symbol means whatever its listener does about it, which makes the semantics a property of the pair. An external anchor moves the semantics somewhere both agents can reach without having met.',
    },
    {
      text: 'Because natural language is more expressive than a discrete message space.',
      correct: false,
      feedback: 'Expressiveness is not the mechanism. A protocol can be highly expressive and still be private to the pair that invented it, which is exactly the problem being solved.',
    },
    {
      text: 'Because the language model translates between the two agents at run time.',
      correct: false,
      feedback: 'No translation happens at run time. The language model generates grounding data during training; the agents then communicate in their own space, which has been shaped to align with the external one.',
    },
    {
      text: 'Because it makes the messages human-readable, and humans can then correct them.',
      correct: false,
      feedback: 'Human readability is a genuine benefit and it is not what makes zero-shot pairing work. The agents are not being corrected by anybody at deployment; they succeed because their protocols were anchored to the same thing.',
    },
  ]}
>
The general pattern is worth recognising: when a learned convention will not
transfer, anchor it to something outside the parties that learned it.
</KnowledgeCheck>

## LangGround Lessons

- The paper starts from the problem this chapter reached: a learned protocol
  is usually **not interpretable to humans, nor to agents not co-trained
  together**, which limits ad hoc teamwork.
- **LangGround** aligns the agents' communication space with an embedding
  space derived from human natural language, grounded on synthetic data
  generated by embodied large language models in teamwork scenarios.
- The authors report that grounding **maintains task performance**,
  **accelerates the emergence** of communication, and yields protocols that
  **generalise zero-shot** to unseen partners and novel task states.
- The general move: when a learned convention's meaning lives only in the pair
  that learned it, anchor it to something external.

## Further reading

**Language Grounded Multi-agent Reinforcement Learning with
Human-interpretable Communication**, Huao Li, Hossein Nourkhiz Mahjoub,
Behdad Chalaki, Vaishnav Tadiparthi, Kwonjoon Lee, Ehsan Moradi-Pari,
Michael Lewis and Katia Sycara. *Advances in Neural Information Processing
Systems 37* (NeurIPS 2024).
[Proceedings page](https://proceedings.neurips.cc/paper_files/paper/2024/hash/a06e129e01e0d2ef853e9ff67b911360-Abstract-Conference.html).
